Através do seu terminal, instale a biblioteca PySimpleGUI com este comando: pip install PySimpleGUI.
Para executar o programa basta abrir o main.py em seu terminal.

Se preferir utilizar o executável disponível em dist e em derivator.rar, saiba que por padrão os antivírus o decectam como ameaça, por causa do formato (.exe). No entanto, este executável não tem nenhum poder ofensivo. Foi gerado através da biblioteca PyInstaller.
